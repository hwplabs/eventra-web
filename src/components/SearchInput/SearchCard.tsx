import React from "react";
import { Box } from "@chakra-ui/react";

const SearchCard: React.FC = () => {
  return (
    <Box width="" bg="white" mt={2} py={4} position="absolute" bottom="-220px">
      {/* Content of the SearchCard */}
    </Box>
  );
};

export default SearchCard;
